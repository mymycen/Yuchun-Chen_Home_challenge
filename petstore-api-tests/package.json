{
  "name": "petstore-api-tests",
  "version": "1.0.0",
  "scripts": {
    "mock": "json-server --watch db.json --port 3001",
    "test": "npm run mock & sleep 1 && jest --runInBand"
  },
  "dependencies": {
    "axios": "^1.6.7"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "json-server": "^1.0.0-beta.3",
    "wait-on": "^8.0.3"
  }
}
